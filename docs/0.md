Awesome! I've created an Implementation Plan outlining exactly how we will split the codebase apart, extract the prompts, and get the folder structure perfectly matching M1-M7 in the system-design.md file.

Please review the plan in the artifact window. If it looks good to you, hit "Proceed" and I'll execute the refactoring immediately!

Implementation Plan
# Refactor Folder Structure & Separation of Concerns

This plan outlines the steps to reorganize the codebase to match the `system-design.md` structure exactly. We are focusing *only* on the folder structure and separation of concerns here, not fixing logic guardrails or API polling yet.

## Proposed Changes

We will create two new directories:
- `backend/modules/`
- `backend/prompts/`

### 1. Modules

#### [NEW] [video_resolver.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/modules/video_resolver.py)
We will extract `extract_video_id` from `transcript.py` and place it here.

#### [NEW] [transcript.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/modules/transcript.py)
We will move the rest of `transcript.py` (the actual fetching logic) into the `modules` directory.

#### [NEW] [claims.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/modules/claims.py)
We will extract the `extract_claims` function from `llm.py` and place it here. It will read its prompt from `prompts/claim_extractor.txt`.

#### [NEW] [evidence_search.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/modules/evidence_search.py)
We will rename and move `search.py` here.

#### [NEW] [evidence_analyze.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/modules/evidence_analyze.py)
We will extract the `rate_claim_with_evidence` function from `llm.py` and place it here. It will read its prompt from `prompts/evidence_summarizer.txt`.

#### [NEW] [evidence_map.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/modules/evidence_map.py)
We will extract the `get_overall_verdict` function from `llm.py` and place it here. It will read its prompt from `prompts/insights_analyzer.txt`.

#### [NEW] [insights.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/modules/insights.py)
We will create a placeholder for this file (M6), which will be implemented fully later.

### 2. Prompts

We will extract the three massive prompt strings currently sitting in `llm.py` into their own text files:
#### [NEW] [claim_extractor.txt](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/prompts/claim_extractor.txt)
#### [NEW] [evidence_summarizer.txt](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/prompts/evidence_summarizer.txt)
#### [NEW] [insights_analyzer.txt](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/prompts/insights_analyzer.txt)

### 3. Cleanup & Integration

#### [MODIFY] [main.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/main.py)
We will update all imports to pull from `modules.*` instead of the root.

#### [DELETE] [llm.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/llm.py)
#### [DELETE] [search.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/search.py)
#### [DELETE] [transcript.py](file:///Users/mayanksharma/Downloads/New_Projects/think-before-share/backend/transcript.py)

## Verification Plan

### Manual Verification
1. I will run `uvicorn main:app --port 8000 --reload` and ensure the server starts up without any import errors.
2. I will verify that `main.py` can still handle an analysis request (the logic itself won't change, just where the code lives).

Done! I have fully completed the structural refactoring. The codebase now matches the system-design.md module and prompt separation exactly.

I've put a summary of exactly what was moved in the walkthrough artifact here: 
walkthrough.md
.

I tested the imports and the Uvicorn server has successfully reloaded without crashing. We are now running on the new modular architecture!

We have successfully knocked out the first item on the Gap Analysis. Would you like to tackle the Error Handling & Guardrails (M2, M3, M5) next, or move straight to refactoring the Orchestration API (M9) to use background polling?

Tasks
# Folder Restructuring Tasks

- [x] Create `backend/modules/` and `backend/prompts/` directories.
- [x] Create `backend/prompts/claim_extractor.txt`.
- [x] Create `backend/prompts/evidence_summarizer.txt`.
- [x] Create `backend/prompts/insights_analyzer.txt`.
- [x] Create `backend/modules/video_resolver.py`.
- [x] Create `backend/modules/transcript.py`.
- [x] Create `backend/modules/claims.py`.
- [x] Create `backend/modules/evidence_search.py`.
- [x] Create `backend/modules/evidence_analyze.py`.
- [x] Create `backend/modules/evidence_map.py`.
- [x] Create `backend/modules/insights.py`.
- [x] Update `backend/main.py`.
- [x] Delete `backend/llm.py`, `backend/search.py`, `backend/transcript.py`.
- [x] Verify server starts up properly.

Walkthrough
# Walkthrough: Folder Restructuring

I have successfully restructured the backend to strictly match the `system-design.md` specifications.

## What Changed

### 1. New Folder Structure
We now have clean separation of concerns with two new directories:
- `backend/modules/`
- `backend/prompts/`

### 2. Extracted Prompts
The massive prompt strings that were hardcoded inside `llm.py` are now clean, editable text files:
- `prompts/claim_extractor.txt`
- `prompts/evidence_summarizer.txt`
- `prompts/insights_analyzer.txt`

### 3. Modular Logic
The monolithic `llm.py`, `transcript.py`, and `search.py` files have been deleted and split into exactly the modules defined by the system design:
- `modules/video_resolver.py` (M1)
- `modules/transcript.py` (M2)
- `modules/claims.py` (M3)
- `modules/evidence_search.py` (M4)
- `modules/evidence_analyze.py` (M5)
- `modules/insights.py` (M6 - *Currently a skeleton, ready to be built*)
- `modules/evidence_map.py` (M7)

### 4. Updated Orchestrator
`main.py` was updated to import from these new namespaces instead of the root directory.

> [!TIP]
> This folder structure makes it much easier to edit prompts without risking syntax errors in python, and ensures that different logic domains (e.g. claims vs evidence search) are isolated.

## What's Next?
We have successfully completed step 1 of the Gap Analysis! The code is much cleaner now. 
I recommend that we tackle the **Error Handling & Guardrails (M2, M3, M5)** next, since those are relatively quick code fixes, before moving on to the larger structural change of the polling API (M9).
